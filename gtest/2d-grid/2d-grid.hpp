#ifndef __2DGRID_HPP__
#define __2DGRID_HPP__

#include <map>
#include <vector>
#include <utility>

std::vector<int> solution(
    std::vector<std::vector<int>> &cards, 
    std::vector<std::vector<int>> &moves, 
    int &query
    );
#endif